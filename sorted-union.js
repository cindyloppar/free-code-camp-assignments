function sortNumbers(arr1, arr2, arr3) {
    var newArray = [];
    for (var i = 0; i < arguments.length; i++) {
        var array = arguments[i];
    }
    for (var K = 0; K < arguments.length; K++) {
        var arr = array[j]
    }
    if (arr.indexOf(arr) < 0) {
newArray.push(arr);
    }
    return newArray;
}

console.log(sortNumbers[1, 3, 2], [5, 2, 1, 4], [2, 1]);