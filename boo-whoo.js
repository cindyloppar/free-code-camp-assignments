function booWho(bool){
    return typeof bool === 'boolean';
    // return (str === true || str === false);
    // if(str === true || str === false){
    //     return true;
    // }else{
    //     return false
    // }
}